{
  "pluginAlias": "ZipaboxPlatform",
  "pluginType": "platform",
  "singular": false,
  "headerDisplay": "Optional content to display above the plugin config. Supports markdown.",
  "footerDisplay": "Optional content to display below the plugin config. Supports markdown.",
  "schema": {
      "username": {
        "title": "Username to my.zipato.com",
        "type": "string",
        "required": true,
        "default": "name@mail.com"
      },
      "password":{
        "title": "Password to my.zipato",
        "type": "string",
        "required": true,
        "default": "****"
      },
      "server_ip": {
        "title": "Local IP or remote",
        "type": "string",
        "required": true,
        "default": "192.168.0.1",
        "description": "Only IP, no http or port. For remote access just type 'remote'"
      },
      "pin": {
        "title": "PIN code for alarm",
        "type": "string",
        "required": false,
        "default": "1234",
        "description": "Need to connect to an alarm partition."
      },
      "refresh": {
        "title": "Refresh rate",
        "type": "integer",
        "required": false,
        "default": 0,
        "description": "Time in seconds to refresh ALL the accessories."
      },
      "debug": {
        "title": "Debug mode ?",
        "type": "boolean",
        "required": false,
        "default": false,
        "description": "Will activate debug for platform and ALL accessories"
      },
      "debugApi": {
        "title": "Debug for API level",
        "type": "boolean",
        "required": false,
        "default": false,
        "description": "Will activate debug for API request."
      },
      "reset": {
        "title": "Reset all accessories",
        "type": "boolean",
        "required": false,
        "default": false,
        "description": "Will ask to the plugin to delete all cached accessories before adding the others."
      },
      "accessories": {
        "title": "Accessories to request on the Box",
        "type": "array",
        "items": {
          "name": "Accessory",
          "type": "object",
          "properties":{
            "name" :{
                "title":"Name",
                "type" : "string",
                "default": "Kitchen light",
                "description": "Name diplayed inside your Home APP",
                "required": true
            },
            "uuid":{
                "title":"uuid",
                "type" : "string",
                "default": "aa1aa1aaa-111a-1a11-11a1-a11111a1a111",
                "description": "UUID of the state. Lowest level in my.zipato (see readme)",
                "required": true
            },
            "type":{
                "title": "Type of accessory",
                "type": "string",
                "default": "switch",
                "oneOf":[
                    {"title":"switch", "enum": ["switch"] },
                    {"title":"contact", "enum": ["contact"] },
                    {"title":"motion", "enum": ["motion"] },
                    {"title":"door", "enum": ["door"] } ],
                "required": true
            }
          }
        }
      }
    }
  }
